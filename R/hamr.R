# Storing all function

vis_missing <- function(df, colour=default, missing_val_char=NA){
    #This function takes a data frame and returns a visualization of all missing values.
    #The missing values are encoded by the missing val character, which is NA by default.
    #
    #Parameters:
    #- data_obj: a data frame or matrix
    #- colour: a colour mapping specification for ggplot2 in R
    #- missing_val_char: This function supports any of the following missing value types: NA, "", "?
}

impute_missing <- function(df, method, missing_val_char) {
# Given a data frame/matrix with missing values, use the specific method to get a complete one
  
}

# Compare_model function
# A summary function that compares summary statistics between various imputation methods
compare_model <- function(feature, methods){
  
}
